============
Installation
============

At the command line::

    $ easy_install django-json-patch

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-json-patch
    $ pip install django-json-patch
